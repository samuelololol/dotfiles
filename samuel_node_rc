echo "Loading nodejs/nvm environment...."
# nvm on linux
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

#TODO: mv nvm to ~/.nvm on mba
# nvm setting on mba
[[ -s "/usr/local/opt/nvm" ]] && source "/usr/local/opt/nvm"

